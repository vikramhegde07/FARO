import React from 'react';

const LoadingAnimation = () => {
    return (
        <div className="loading-overlay">
            <div className="spinner"></div>
            {/* You can add text like "Loading..." here */}
        </div>
    );
};

export default LoadingAnimation;